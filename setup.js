var currentUpdate = new Event('onCurrentUpdate'); // custom update event

$(document).ready(function() {

	/*$(document).on('click', '#gazeTracker', function(evt) {
		// run gaze tracker script...
	});

	$(document).on('click', '#lyricTracker', function(evt) {
		// run lyric tracker script...
	});

	$(document).on('click', '#chordTracker', function(evt) {
		// run chord tracker script...
	});*/

});
